Question - You just need to take two strings as an input and check later string is the suffix of the former one. If yes, return a string 'True' else return 'False'.
Sol- 
import java.io.*;
import java.util.*;
public class CandidateCode {
   public static void main(String args[] ) throws Exception {
Scanner scan=new Scanner (System.in);

String txt=scan.nextLine();
String pat=scan.next();
int l=pat.length();
int N=txt.length();

if(txt.substring(N-l,N).equals(pat))
{System.out.print("True");}
   else
   System.out.print("False");
   
   }
}
